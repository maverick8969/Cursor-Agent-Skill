# Archived Agents

These agents were moved out of `.cursor/agents` during curation Phase 2 to reduce active selection noise while preserving a fast rollback path.

If you want to restore an agent, move its `.md` file back into `.cursor/agents/`.
